# NTUH phone

加密的離線查詢頁。內容以 **AES-GCM-256** 加密，金鑰由通行碼經
**PBKDF2-SHA256（600,000 次）** 導出，解密完全在瀏覽器內完成。

這個 repo 只有密文；沒有通行碼就只會看到解鎖畫面。
通行碼不會離開你的裝置，也沒有存在任何伺服器上。

安裝：用手機瀏覽器開啟後，Safari 選「加入主畫面」、Chrome 選「安裝應用程式」。
勾「記住這台裝置」後，之後開啟不必再輸入通行碼（金鑰存在該裝置的 localStorage）。

內容的產生與更新在另一個 private repo。
